info:
  name: Get Tasks
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:3000/api/tasks/${projectId}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
