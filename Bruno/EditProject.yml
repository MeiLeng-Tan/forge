info:
  name: EditProject
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/projects/:projectId/edit
  params:
    - name: projectId
      value: 69d9c335393265f6f8fa4367
      type: path
  body:
    type: json
    data: |-
      {
        "targetDate": "2026/01/01",
        "status": "In Review"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
